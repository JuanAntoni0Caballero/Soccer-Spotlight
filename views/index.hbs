<div id="indexBody">
    <div id="init" class="rounded text-center p-5">
        <h1 class="mb-3">{{appTitle}}</h1>
        {{#if user}}
        <p>Welcome {{user.username}}!</p>
        {{else}}
        <p>Welcome visitor!</p>
        {{/if}}
    </div>
</div>